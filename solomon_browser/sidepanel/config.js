// solomon_browser/sidepanel/config.js
export const config = {
    // Backend URL must be configurable and not hardcoded to a local IP
    backendTargetUrl: "https://api.example.com",
    version: "1.0.0"
};