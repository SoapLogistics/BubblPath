export const config = {
    // Backend URL must be configurable and not hardcoded to a local IP
    // The default is a local URL which can be updated dynamically
    backendTargetUrl: "http://127.0.0.1:8000",
    version: "1.0.0"
};
