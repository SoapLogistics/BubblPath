# Current State of Solomon Card System

## Executive Summary
Through thorough reverse-engineering of the **Solomon / OpenClaw operating workspace** and surrounding architectural blueprints, we have documented the existing card-based protocol ecosystem. This system codifies operational doctrine, autonomy rules, and self-evolution procedures.

While the small Flask application (`app.py`) serves as a stateless gateway, the actual autonomy workspace is defined by a modular, document-driven architecture. This document provides a complete breakdown of the existing card architecture, the lifecycle of these cards, and the distinction between theoretical specs and active code implementations.

---

## 1. Discovered Card Types

In the Solomon ecosystem, **"Cards"** are structured units of instruction, work, or knowledge. Currently, these are categorized into several specialized forms:

| Card Type | Format / Representation | Purpose |
| :--- | :--- | :--- |
| **Identity & Soul Cards** | `IDENTITY.md`, `SOUL.md` | Core behavioral traits, alignment guardrails, and operational boundaries of Solomon. |
| **Task Cards** | Queue Task JSON / `autonomous_cycle.md` | Units of execution representing individual steps, inputs, and targeted outputs for workers. |
| **Procedure Cards (PC)** | `checklists/*` or specialized Python schemas | Standardized, reusable playbooks (e.g., `PC-SO-01`, `PC-SO-02`) that guide high-level agent routines. |
| **Worker Reports** | Action Log JSON / Markdown summaries | Historical execution reports generated by workers after executing a task card. |
| **Review Packets** | SS3 Review Schema JSON | Evaluation packages used to assess worker performance and validate lessons before codification. |
| **Registries & Indexes** | `solomon_oss_first_pass_registry.json` | Index maps used to discover, sort, and retrieve procedural assets. |

---

## 2. Evolution of the Card System
The card system has evolved through three distinct evolutionary stages:

1.  **Stage 1: The Task Card (Ephemeral State):**
    *   *Origin:* Simple JSON instructions pushed to a queue.
    *   *Characteristics:* High granularity, single-use, stateless, and focused on executing a brief discrete command.
2.  **Stage 2: Operational Checklists (Document-Driven Doctrine):**
    *   *Origin:* Transitioning checklists into active Markdown-based operating rules (`autonomous_cycle.md`, `openhands_integration.md`).
    *   *Characteristics:* Human-readable, structured, but decoupled from runtime execution engines.
3.  **Stage 3: The Procedure-Card Factory (Automated Synthesis):**
    *   *Origin:* `solomon_procedure_card_factory.py`.
    *   *Characteristics:* Programmatic generation, semantic indexing (`solomon_procedure_index.json`), and structured versioning enabling Solomon to dynamically package, parameterize, and write its own procedures.

---

## 3. Discovered Card Schemas & Fields

### A. Procedure Card (PC) Schema (e.g., PC-SO-01, PC-SO-02)
Procedure cards represent Solomon's reusable operational playbooks:
```json
{
  "procedure_id": "PC-SO-XX",
  "title": "String",
  "domain": "Enum (COGNITION, SYSTEM_EVOLUTION, INTEGRATION, ABSORPTION)",
  "version": "SemanticVersion",
  "dependencies": ["Array of Procedure IDs"],
  "trigger": {
    "type": "Enum (CRON, EVENT, MANUAL)",
    "condition": "String"
  },
  "steps": [
    {
      "step_number": "Integer",
      "action": "String",
      "required_tools": ["Array of Tool Names"],
      "expected_outcome": "String"
    }
  ],
  "telemetry": {
    "metrics_file": "String (e.g., 'growth_metrics.json')",
    "logs_file": "String (e.g., 'heartbeat.log')"
  }
}
```

### B. Task Card Schema
Task cards dictate individual runs dispatched to worker agents:
```json
{
  "task_id": "UUIDv4",
  "parent_procedure": "String (PC-SO-XX)",
  "agent_role": "String (e.g., 'OpenSourceAbsorber')",
  "inputs": {
    "target_repository": "String",
    "absorb_depth": "Integer"
  },
  "guardrails": {
    "max_iterations": "Integer",
    "timeout_seconds": "Integer"
  }
}
```

---

## 4. Solomon's Cognitive Cycle & Card Flow
The flow of intelligence through Solomon is circular, turning ephemeral execution events into permanent procedural memory:

```
        ┌────────────────────────────────────────────────────────┐
        ▼                                                        │
┌──────────────┐      ┌──────────────┐      ┌──────────────┐     │
│  Procedure   │ ───> │  Task Card   │ ───> │  Worker Run  │     │
│  Card (PC)   │      │ (Queue JSON) │      │  (Execution) │     │
└──────────────┘      └──────────────┘      └──────┬───────┘     │
                                                   ▼             │
┌──────────────┐      ┌──────────────┐      ┌──────────────┐     │
│  SOP Reuse   │ <─── │   Review /   │ <─── │  Worker Log  │ ────┘
│  & Learning  │      │  Validation  │      │   & Report   │
└──────────────┘      └──────────────┘      └──────────────┘
```

1.  **Queue Activation:** A high-level *Procedure Card* triggers the generation of discrete *Task Cards*.
2.  **Execution:** The task is pushed to the worker queue. A specialized worker executes the steps.
3.  **Logging:** The worker produces a detailed *Worker Report* capturing telemetry, outputs, and errors.
4.  **Review (SS3 Protocol):** A *Review Packet* is generated, pairing the execution report with original guardrails.
5.  **Codification:** If validated, any deviations or lessons are codified. The *Procedure-Card Factory* updates the base Procedure Card, completing the feedback loop.

---

## 5. Short-Term State vs. Long-Term Knowledge
We distinguish between short-term state vectors and long-term operating parameters:

*   **Short-Term State (Ephemeral & Dynamic):**
    *   *Components:* Active Queue Tasks, Worker Reports, current process PIDs, `heartbeat.log` updates.
    *   *Purpose:* Tracks execution context, immediate progress, and transient system states. Discarded or summarized once the task terminates.
*   **Long-Term Knowledge (Static & Persistent):**
    *   *Components:* Identity and Soul files (`IDENTITY.md`, `SOUL.md`), Procedure Cards, `solomon_oss_first_pass_registry.json`.
    *   *Purpose:* Guides overarching behavior, safety limits, architectural rules, and standardized execution playbooks across multi-day lifecycles.

---

## 6. Implementation Gap Analysis: Core vs. Workspace
An honest evaluation reveals a separation between the defined **Operational Doctrine** and the **Executable Engine**:

*   **The Operational Doctrine (Implemented & Verified):**
    *   Solomon's identity, core rules, checklist-driven workflows, and step-by-step procedures (`PC-SO-01`, `PC-SO-02`) are fully codified in Markdown within the OpenClaw workspace.
*   **The Executable Engine (Partially Implemented / Specification Only):**
    *   The orchestration code to automatically generate, search, dynamically link, and recursively retrieve cards based on semantic vector embeddings is not present in the current local runtime. It remains a specification that needs to be actively developed.

---

## 7. Categorized Capability Maturity Matrix

The table below catalogs each component of the Solomon Card Ecosystem by its current maturity state:

| Component | Status | Description |
| :--- | :--- | :--- |
| **Operational Checklists** | **Running & Verified** | Markdown files detailing the exact execution paths for CrewAI, OpenHands, and Autonomy cycles. |
| **Identity & alignment rules** | **Running & Verified** | Core `IDENTITY.md`, `SOUL.md`, and `USER.md` instructions loaded by agents. |
| **Procedure Index / Map** | **Partially Implemented** | A tracking JSON of available procedures, requiring manual verification during updates. |
| **Procedure-Card Factory** | **Partially Implemented** | Python utility code capable of programmatically instantiating card templates. |
| **Task Queue Schema** | **Specification Only** | Structuring schema definitions for inputs and outputs of autonomous cycles. |
| **Automated Semantic Search**| **Specification Only** | Real-time hybrid search/vector embedding-driven retrieval prior to task runs. |
| **Asynchronous Reflection** | **Specification Only** | Post-task automated log parser designed to generate new Failure and Repair cards. |
| **Confidence Feedback Loop** | **Specification Only** | Auto-incrementing and auto-decrementing confidence levels based on run successes. |
