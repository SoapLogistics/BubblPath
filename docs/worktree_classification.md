# Worktree Classification Note

- Source files (`services/`, `backend/services/`, `solomon_api/`) are classified in `solomon_api/engine_registry.json`.
- Documentation is stored in `docs/`.
- Tests are stored in `tests/`, `backend/tests/`, etc.
- Ignore cache files, logs, and generated artifacts like `*.db` and `__pycache__/`.
