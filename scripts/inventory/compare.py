def run_compare(current_inventory, previous_inventory=None):
    # In a real environment we'd load previous from LATEST and compare
    # Here we just return a stub structure
    return {
        "status": "baseline_established",
        "differences": []
    }
