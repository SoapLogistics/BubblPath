services:
  - type: web
    name: chatgpt-flask-app
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn "app:create_app()" -b 0.0.0.0:10000
    envVars:
      - key: OPENAI_API_KEY
        sync: false
