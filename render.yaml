services:
  - type: web
    name: chatgpt-flask-app
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 4 -k gevent -b 0.0.0.0:10000 app:app
    envVars:
      - key: OPENAI_API_KEY
        sync: false
